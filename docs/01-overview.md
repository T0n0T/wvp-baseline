# 总览

本仓库用于在一台 `powerpc64le` 主机上统一管理如下项目：

- `WVP-GB28181-Pro`
- `ZLMediaKit`

目标：

1. 以 git 子模块方式固定源码基线。
2. 用脚本收敛构建、配置、启动、停止、信息收集入口。
3. 将运行时目录统一收敛到生成态 `.runtime/`，避免直接污染源项目 `docker/` 目录。
4. 用文档记录镜像构建、部署与配置方案。
5. 为后续继续收敛精简镜像、配置模板和自动化验证提供基础。
