# Agent Orchestration（项目级）

## Step-by-Step Confirmation

每个任务都必须分步执行,并在每一步的边界停下来等待人工确认:

1. **先拆分**:执行前用 TaskCreate/TodoWrite 把任务拆成明确的步骤。
2. **每步停下**:完成一步后 STOP,向用户汇报:
   - 本步完成了什么
   - 下一步要做什么
   - 继续前需要什么(决策、输入、批准)
3. **等确认再继续**:不要自主连跑多步,只有用户确认后才进入下一步。
4. 单个已确认的步骤内部,并行子代理执行(见全局 agents.md 的 Parallel Task Execution)仍然适用。

```
# BAD: 拆好步骤后一口气跑完
步骤1 → 步骤2 → 步骤3 中间不停顿

# GOOD: 每个边界停下确认
步骤1 → 汇报结果+下一步 → 等确认 → 步骤2 → ...
```
